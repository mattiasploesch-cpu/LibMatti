// Port of net.neoforged.fml.common.asm.enumextension.IExtensibleEnum.
// Java declares an empty interface; the port only needs its type descriptor, which lives in the
// header so that RuntimeEnumExtender can match on the enum's interface list.

#include "libmatti/net/neoforged/fml/common/asm/enumextension/IExtensibleEnum.h"
